# Gemfile
source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'rackup'

# Adicione UM destes servidores:
gem 'puma'           # Recomendado - mais performático
# gem 'thin'         # Alternativa leve
gem 'webrick'      # Servidor padrão do Ruby (já vem instalado)